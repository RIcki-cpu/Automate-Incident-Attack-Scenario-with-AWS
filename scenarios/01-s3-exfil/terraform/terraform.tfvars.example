# Copy this file to terraform.tfvars and adjust if you want non-default
# values. terraform.tfvars is gitignored — never commit it.
#
# Every variable in variables.tf currently has a default, so this file is
# optional for Scenario 01; it exists so you have somewhere obvious to
# override things like the region.

aws_region = "us-east-1"
